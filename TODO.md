# TODO

## Map

- [ ] Cap camera minimum zoom distance — tileset has no tiles below ~level 20 (~35m altitude). Add `screenSpaceCameraController.minimumZoomDistance` in the viewer setup `useLayoutEffect` in `Map3D.tsx`.
